export enum LeadFlowAgentType {
  Reception = 'reception',
  Qualifier = 'qualifier',
  Scheduler = 'scheduler',
  Sales = 'sales',
  Support = 'support',
  Concierge = 'concierge',
  Custom = 'custom',
}
