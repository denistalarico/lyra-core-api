export enum PlatformProductKey {
  Agency = 'agency',
  LeadFlow = 'leadflow',
  Social = 'social',
}

export enum ProductEntitlementStatus {
  Active = 'active',
  Trial = 'trial',
  Suspended = 'suspended',
  Expired = 'expired',
  Cancelled = 'cancelled',
}

export enum ProductEntitlementSource {
  Subscription = 'subscription',
  Manual = 'manual',
  Trial = 'trial',
  Promotion = 'promotion',
  Migration = 'migration',
  Compatibility = 'compatibility',
}
