export enum PlatformAccountType {
  Agency = 'agency',
  Business = 'business',
  Internal = 'internal',
}

export enum PlatformAccountStatus {
  Active = 'active',
  Suspended = 'suspended',
  Archived = 'archived',
}

export enum PlatformOnboardingMode {
  Agency = 'agency',
  OwnBusiness = 'own_business',
  Internal = 'internal',
  Migration = 'migration',
}
