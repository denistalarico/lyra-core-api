export enum TaskStatus {
  Todo = 'todo',
  InProgress = 'in_progress',
  InReview = 'in_review',
  Approved = 'approved',
  Waiting = 'waiting',
  Blocked = 'blocked',
  Done = 'done',
  Cancelled = 'cancelled',
  Archived = 'archived',
}
